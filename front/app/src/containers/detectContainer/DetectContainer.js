import React, {Component} from 'react';
import { DetectWrapper } from '../../components';

class DetectContainer extends Component {
    render() {
        return (
            <DetectWrapper>
            </DetectWrapper>
        );
    }
}

export default DetectContainer;